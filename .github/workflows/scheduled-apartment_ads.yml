name: Run python script every day at 00.02

on:
  schedule:
    # Run every day at 3:00 AM UTC
    - cron: '1 23 * * *'  

  workflow_dispatch:

jobs:
  run-python-script:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.9'
          
      - name: Install dependencies
        run: |
          cd dlt_setup
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Run the Python script
      - name: Run apartmentads.py script
        env:
          SNOWFLAKE_USERNAME: ${{ secrets.username }}
          SNOWFLAKE_PASSWORD: ${{ secrets.password }}
          SNOWFLAKE_HOST: ${{ secrets.host}}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.warehouse }}
          SNOWFLAKE_DATABASE: ${{ secrets.apartment_watcher }}
          SNOWFLAKE_SCHEMA: ${{ secrets.schema }}
          SNOWFLAKE_ROLE: ${{ secrets.role }}
        run: |
          cd dlt_setup
          python apartmentads.py
